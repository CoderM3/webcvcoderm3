[build]
builder = "NIXPACKS"

[deploy]
startCommand = "composer install --no-dev && npm ci && npm run build && touch database/database.sqlite && php artisan migrate --force && php artisan config:cache && php artisan serve --host=0.0.0.0 --port=$PORT"
